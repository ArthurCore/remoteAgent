# AW-008A2b Artifact Builders Quality/Security Closure — xhigh

## Scope

- Re-reviewed only H1/H2/M1/M2/L1 from `aw-008a2b-artifact-builders-quality-security-review-xhigh.md` against baseline `1182bb6` plus current uncommitted A2b1.
- A2b2 generation, committed bytes, write/check behavior, exports, and wiring remain out of scope.

## Finding closure

### H1 — RESOLVED: metadata isolation and structural-key rejection

- Core conversion supplies an empty private metadata registry (`packages/contracts/src/artifacts.ts:560-580`, especially line 570), so ambient `z.globalRegistry` metadata is not consumed.
- Full conversion prewalks the schema graph, copies only the explicit annotation allowlist into a private registry, validates root/nested IDs, and rejects symbol, structural, and other unapproved keys with paths (`artifacts.ts:319-328,379-517`); all three conversions use that registry (`artifacts.ts:805-836,870-885`).
- The completed document rejects non-local/dangling refs and `$id`/anchor/dynamic structural keywords (`artifacts.ts:921-1006`). Tests cover unchanged ambient poison, allowed annotations, root/child structural keys, safe/reserved/duplicate IDs, and ref resolution (`packages/contracts/test/artifacts.spec.ts:405-431,577-688`).

### H2 — OPEN: hostile fixed-property `__proto__` is still corrupted

- Safe component-ID grammar/collision checks, null-prototype post-normalization maps, own-key collision checks, and final local-ref validation close the reported named-ID/ref cases (`artifacts.ts:328,420-447,535-549,574-595,849-860,928-1006,1012-1034,1120-1129`).
- However, the prewalk accepts every own fixed-property name (`artifacts.ts:457-475`) and calls Zod before null-prototype normalization (`artifacts.ts:805-868`). An independent probe using `z.object(Object.fromEntries([["__proto__", z.string()]])).strict()` produced `propertyKeys: []` with `required: ["__proto__"]`; the final `properties` prototype was null, but conversion had already lost the own schema entry.
- This publishes an unsatisfiable/corrupted state contract instead of safely preserving or rejecting a legal JSON key. The current prototype test uses only a safe fixture and merely asserts no own `__proto__` (`artifacts.spec.ts:557-575`); the hostile-name matrix tests component IDs, not fixed properties (`artifacts.spec.ts:662-672`). Reject unsafe fixed-property keys before conversion or ensure conversion itself uses own-key-safe maps, then add JSON Schema/OpenAPI regression coverage.

### M1 — RESOLVED: exported configuration is runtime-immutable

- Registry and generic-factory arrays and every entry are frozen (`artifacts.ts:70-267`); schemas themselves are intentionally not frozen.
- The mutation test proves ESM writes throw and repeated core/full schema/OpenAPI bytes remain unchanged (`artifacts.spec.ts:294-330`).

### M2 — RESOLVED: schema graph budgets are enforced before conversion

- Frozen limits are 4096 unique nodes, 4096 fixed properties, and nesting 128 (`artifacts.ts:277-281`); the visited graph walk enforces them before `z.toJSONSchema` while allowing recursion (`artifacts.ts:379-517`).
- Tests cover each over-limit error and recursive root/named positives (`artifacts.spec.ts:690-778`). Independent exact-boundary probes accepted 4096 fixed properties, 4096 unique nodes, and nesting 128; the suite's above-boundary probes reject with path-bearing `TypeError`s.

### L1 — RESOLVED: renderer cycle/depth failures are stable

- The renderer uses an active-path `WeakSet`, `finally` cleanup for shared DAGs, and a depth-256 guard (`artifacts.ts:1168-1208`).
- Self/mutual cycles, shared DAGs, and depth 257 are covered (`artifacts.spec.ts:892-930`); an independent depth-256 probe succeeded.

## Verification and disposition

- Focused artifact suite: 52/52 passed; all contracts tests: 236/236 passed.
- Contracts typecheck, focused ESLint, and focused Prettier check passed. The independent safe-ID boundary accepted length 65 and rejected length 66.
- Remaining A2b1 severities: BLOCKER 0, HIGH 1 (H2), MEDIUM 0, LOW 0.
- A2b2 still owns generator execution, committed generated bytes, atomic writes, no-write drift/check mode, exports/root-index integration, and Vitest wiring. It must not normalize or bless the malformed A2b1 property output.

Verdict: REQUEST_CHANGES
