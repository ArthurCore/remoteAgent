# AW-008A2b Artifact Builders Quality/Security Final Closure — xhigh

## Scope

- Re-reviewed only H2 from `aw-008a2b-artifact-builders-quality-security-closure-xhigh.md` against baseline `1182bb6` plus the current uncommitted A2b1 final fix.
- H1, M1, M2, and L1 remain resolved as closed at `aw-008a2b-artifact-builders-quality-security-closure-xhigh.md:10-35`; no A2b2 implementation was reviewed.

## Finding closure

### H2 — RESOLVED: hostile fixed-property names fail closed before conversion

- The rejection policy is the exact set `__proto__`, `constructor`, `prototype`, and `toString` (`packages/contracts/src/artifacts.ts:329`), not a prefix or substring rule.
- The metadata/budget prewalk visits each unique schema, inspects every visited object shape's own enumerable keys, forms the exact escaped JSON Pointer, rejects a listed key with `TypeError`, and recursively visits child schemas (`artifacts.ts:388-397,450-524`, especially `458-481`).
- `convertStrictSnapshotState` completes that prewalk at line 816 before either `z.toJSONSchema` call at lines 821-838, so the prior Zod corruption cannot occur first.
- The four-key matrix uses `Object.fromEntries` and requires path-bearing rejection through both JSON Schema and OpenAPI builders (`packages/contracts/test/artifacts.spec.ts:577-592`).
- An independent root/nested × four-key × two-builder probe produced 16/16 path-bearing `TypeError`s, including nested paths such as `#/properties/nested/properties/__proto__`; no hostile case reached conversion.

## No overbroad near-name regression

- Exact `Set.has` membership leaves `__proto___safe` admissible. Its `Object.fromEntries` regression proves an own `{ type: "string" }` property and resolves all JSON Schema/OpenAPI refs (`artifacts.spec.ts:594-608`).
- The focused suite passed that regression; an independent probe also reported `safeOwn: true`, `strict: true`, the exact string schema, and successful OpenAPI construction.

## Remaining severity and A2b2 boundary

- Remaining A2b1 severities: BLOCKER 0, HIGH 0, MEDIUM 0, LOW 0. No reviewed quality/security finding remains open.
- A2b2 still owns generator execution, committed generated bytes, atomic writes, no-write drift/check mode, exports/root-index integration, and Vitest wiring; none is a reopened A2b1 defect.

## Verification

- Focused artifacts: 57/57 passed; all contracts: 241/241 passed.
- Contracts typecheck and build, focused ESLint, focused Prettier, and `git diff --check` passed; build output was cleaned and `dist` is absent.
- A2b1 may merge unchanged.

Verdict: APPROVED
