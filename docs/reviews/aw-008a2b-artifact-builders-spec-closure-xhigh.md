# AW-008A2b Artifact Builders Spec Closure — xhigh

## Scope

- Re-reviewed only the prior HIGH/MEDIUM/LOW findings in `aw-008a2b-artifact-builders-spec-review-xhigh.md` against A2b1 at baseline `1182bb6` plus the uncommitted corrections.
- A2b2 generator scripts, committed generated artifacts/byte-drift checking, exports/root-index wiring, and Vitest wiring remain outside this closure.

## Finding closure

### HIGH — RESOLVED: nested fixed-shape objects are fail-closed

- `artifacts.ts:449-469,471-535` walks schema maps, arrays/combinators, and schema-valued keywords (including `$defs`, `properties`, `items`, and `additionalProperties`); every node bearing `properties` must set `additionalProperties: false`, with its exact JSON Pointer in the error.
- Both input and output state documents are validated before publication (`artifacts.ts:549-583`).
- The negative matrix rejects root open/loose and nested default-open, loose, empty-loose, catchall, array/union, named-open, and typed-map-value-open schemas through both builders, with path assertions (`artifacts.spec.ts:507-591`).
- The generated-bundle probe rejects unknown root-state and nested-member fields (`artifacts.spec.ts:420-451`).

**Typed-map distinction confirmed:** a map container without `properties` is allowed, while its `additionalProperties` value schema is traversed. The positive strict-value map and ref-resolution test is `artifacts.spec.ts:484-505`; the open fixed-shape map-value rejection is `artifacts.spec.ts:553-560`.

### MEDIUM — RESOLVED: positive named nested refs are covered

- The strict non-reserved named fixture is defined at `artifacts.spec.ts:77-90`.
- State `$defs` are collision-checked, normalized, hoisted, and inserted into the bundle at `artifacts.ts:585-604,660-669`; the wrapper state is rewritten to `#/$defs/ChannelReplicaStateV1` at `artifacts.ts:623-635`.
- `artifacts.spec.ts:454-482` asserts the state-to-named ref, named definition strictness, wrapper/state ref, OpenAPI external component ref, and resolution of every core/full JSON Schema and OpenAPI ref via `expectAllRefsResolve` (`artifacts.spec.ts:142-200`).

### LOW — RESOLVED: output-direction substitution is guarded

- The sole exception list is documented and frozen as exactly `DeltaResponseV1` (`artifacts.ts:209-215`; `artifacts.spec.ts:259-291`).
- Registry generation compares normalized input/output definition sets and every definition, rejects stale/unimplemented exceptions, and fails closed on any unexplained divergence (`artifacts.ts:293-357,401-440`).
- The Delta exception requires input `items` to be exactly `{}`, output `items` to be exactly the `SyncItemV1` array with `maxItems: 500`, and all remaining definition content to equal input (`artifacts.ts:359-399`).
- A substituted defaulted schema proves both JSON Schema and OpenAPI builders reject unapproved direction divergence (`artifacts.spec.ts:293-308`).

## Remaining severity and boundary

- Remaining A2b1 HIGH: none. MEDIUM: none. LOW: none. No prior finding remains OPEN.
- A2b2 still owns generator execution/check mode, committed generated bytes and drift enforcement, exports/root-index integration, and Vitest wiring; their absence is not reopened here.

## Verification

- Focused artifacts: 28/28 passed; all contracts: 212/212 passed.
- Contracts typecheck, focused ESLint, Prettier check, and `git diff --check` passed.

A2b1 may proceed unchanged to quality review.

Verdict: PASS
