# AW-008G Specification Closure — xhigh

## Verdict

**PASS — authorize the evidence-only closure commit.** No AW-008 specification defect remains at verified implementation head `17442e6a4957c81786415b3164c936b4add2573e`.

This is not authorization to mark AW-008 DONE or merge yet. The blocking PR workflow must succeed once more on the eventual evidence-only final PR head, after this review and the separate quality/security closure are committed. The three real hosted F1 successes at `2bbb0f1016a37947e9f3172f64c0a4ed1078a7df` close the prior F1 hosted-proof defect; they do not masquerade as proof for the later property-test/evidence head.

## Review basis

I adjudicated every acceptance item in plan §11 and every closure-checklist item in §13 (`docs/plans/aw-008-contracts-db-foundation.md:198-205`, `docs/plans/aw-008-contracts-db-foundation.md:240-248`). I used the retained handoff, the F1 hosted-run closure, bounded source/evidence spot checks, current status/diff, and immutable hashes; I did not substitute a new slow test or Compose run for the retained evidence.

At review time, HEAD was exactly `17442e6a4957c81786415b3164c936b4add2573e`; `git status --short` contained only the four reserved untracked review/evidence documents and no tracked implementation change. `git diff --check` passed. Independent static checks reproduced the two generated-artifact byte counts/hashes, all three migration hashes, exactly six foundation tables, no message/event/outbox/idempotency/projection migration table, exactly nineteen root scripts, and the canonical root script bodies recorded in the handoff.

## §11 acceptance closure

1. **Frozen tree, contracts, artifacts, and boundaries — PASS.** The final frozen install and uncached local CI passed with cache zero; contracts were `190/190`, DB unit `64/64`, and artifact tests `81/81` (`docs/reviews/aw-008g-full-evidence-handoff-xhigh.md:11-29`). The six deterministic properties freeze seeds `8008001..8008006` and `256/256/128/64/512/512` runs (`packages/contracts/test/primitives.spec.ts:14-22`), with explicit accepted/rejected ID, cursor, and canonical BIGINT buckets (`packages/contracts/test/primitives.spec.ts:49-176`). The public runtime oracle freezes exactly 35 names (`packages/contracts/test/artifacts.spec.ts:88-124`, `packages/contracts/test/artifacts.spec.ts:438-447`), matching the root exports (`packages/contracts/src/index.ts:5-46`). Independent hashing reproduced OpenAPI `6457` bytes / `cf19e093...b844ea` and JSON Schema `27323` bytes / `5660df23...a7882` exactly as retained (`docs/reviews/aw-008g-full-evidence-handoff-xhigh.md:22-29`). Tree, boundary negative fixture, six-table scaffold, and nineteen-script checks passed in the same uncached lane (`docs/reviews/aw-008g-full-evidence-handoff-xhigh.md:15-21`).

2. **Real PostgreSQL/Testcontainers — PASS.** The retained digest-pinned PostgreSQL run passed three files and `25/25`, including first/second/concurrent/failing/hash-drift migrations, exact DDL/access negatives, runtime DDL/ledger denial, and cleanup (`docs/reviews/aw-008g-full-evidence-handoff-xhigh.md:31-42`). The suite expects only the six named tables and exercises first/no-op, serialized runners, bootstrap denial, transactional failure, and hash drift (`packages/db/test/migration.integration.spec.ts:34-41`, `packages/db/test/migration.integration.spec.ts:323-505`). Exactly two valid evidence JSON files were mode `0600`, credential-pattern matches were zero, and harness residue was zero (`docs/reviews/aw-008g-full-evidence-handoff-xhigh.md:33-40`).

3. **Actual public PR lane — PASS for F1; final-head rerun required.** Run `32872753268` had three SUCCESS attempts at the same immutable PR head `2bbb0f1`, with distinct real jobs/runners and downloaded Testcontainers evidence (`docs/reviews/aw-008f1-public-pr-workflow-closure-xhigh.md:22-37`). The workflow remains read-only, frozen, uncached, digest/SHA pinned, and fails on missing evidence (`.github/workflows/ci.yml:3-39`). Those hosted logs correctly show contracts `184/184`; the later contracts-test-only property correction produces local `190/190`. Therefore the final evidence-only PR head must run the blocking workflow once more before merge/DONE (`docs/reviews/aw-008g-full-evidence-handoff-xhigh.md:52-68`, `docs/reviews/aw-008g-full-evidence-handoff-xhigh.md:113-120`).

4. **Cold seven-service Compose — PASS.** The topology is exactly PostgreSQL, RustFS, storage init, migrator, API, worker, and Web, with migrator completion gating API/worker and hardened non-root app services (`docker-compose.yml:20-31`, `docker-compose.yml:32-190`). Retained proof at `17442e6` covers cold build/wait, all health contracts, both one-shots exit 0, role/ledger/hash/DDL checks, forced migrator recreation plus a second smoke, and teardown `0/0/0` (`docs/reviews/aw-008g-full-evidence-handoff-xhigh.md:70-94`). The immutable final image is `sha256:03efb82aff0d07d8fa1e2de0c891413a10088a5eee4bfa00d89b34773d1feec6`, 100,854,235 bytes, revision `17442e6` (`docs/reviews/aw-008g-full-evidence-handoff-xhigh.md:86-93`).

5. **Supply-chain/security artifacts and diff — PASS with exact scanner interpretation.** The stable 171-file candidate, including the reserved review paths, had raw Gitleaks zero and Trivy secrets zero. The reachable-history Gitleaks prose match is explicitly an adjudicated historical false positive; reachable history is not claimed scanner-zero (`docs/reviews/aw-008g-full-evidence-handoff-xhigh.md:96-111`). Trivy addressed the immutable image ID and proved only the specified fixed Critical gate (`--severity CRITICAL --ignore-unfixed`) was zero; it is not a claim of zero findings at every severity or among unfixed findings. Syft addressed that same ID and produced parsed CycloneDX 1.7 with 3,538 components and metadata version equal to the image digest (`docs/reviews/aw-008g-full-evidence-handoff-xhigh.md:100-103`).

6. **Bare migration fence — PASS.** With both migration variables and ambient `DATABASE_URL` unset, the CLI exited 2 with the generic configuration error before network access and without URL/secret markers (`docs/reviews/aw-008g-full-evidence-handoff-xhigh.md:42-42`). Source parsing requires `MIGRATION_DATABASE_URL` plus one exact target class and never reads runtime `DATABASE_URL` (`packages/db/src/migration-env.ts:1-60`); the CLI parses that environment before creating a pool (`packages/db/src/migrate.ts:159-180`). This satisfies the minted Testcontainers/Compose URL-only requirement.

## §13 closure checklist

- **D1 contracts — PASS.** `ActorV1` remains the one strict `{principal_id,kind}` object; the normative payload freeze defines all seven exact discriminants/field sets (`docs/contracts/durable-event-payloads-v1.md:17-38`, `docs/contracts/durable-event-payloads-v1.md:40-101`). Runtime schemas implement those strict payloads and the exact seven-way `DurableEventV1` union (`packages/contracts/src/events.ts:81-138`, `packages/contracts/src/events.ts:140-196`); `SyncItemV1.event` is that production union, not the abstract envelope (`packages/contracts/src/sync.ts:3-21`). Export, registry, JSON Schema/OpenAPI reference, strictness, and byte-parity evidence is exact as cited above.

- **D2 six-table/AW-010 boundary — PASS.** The schema contains exactly `tenants`, `workspaces`, `principals`, `workspace_memberships`, `channels`, and `channel_membership_epochs`, with tenant-leading composite keys/FKs/indexes and the complete epoch checks/partial unique index (`packages/db/src/schema/foundation.ts:16-217`). Independent migration scanning found no message, durable-event, outbox, idempotency, or projection table. The no-write sequence fence remains explicit: synthetic structural markers are not product write authority; AW-009 must resolve the committed join-sequence prerequisite and AW-010 cannot invent it (`docs/plans/aw-008-contracts-db-foundation.md:141-143`, `docs/reviews/aw-008g-full-evidence-handoff-xhigh.md:105-110`). Event *contracts* do not cross this persistence/behavior boundary.

- **D3 Testcontainers policy — PASS.** The harness fixes the approved PostgreSQL image digest, generated resources/credentials and ownership labels (`packages/db/test/support/postgres.ts:12-29`, `packages/db/test/support/postgres.ts:154-184`), performs stale-label cleanup and explicit per-run removal (`packages/db/test/support/postgres.ts:321-345`, `packages/db/test/support/postgres.ts:495-523`), uses Testcontainers host/mapped port, records actual Docker HostIp, and writes credential-filtered evidence mode `0600` (`packages/db/test/support/postgres.ts:598-722`). Fresh reset, serial suite, Reaper-backed lifecycle, retained resources, and residue zero are proven by the `25/25` evidence above.

- **D4 migration semantics — PASS.** The runner holds one session advisory lock across pre-integrity, migrate, and post-integrity, then aggregates unlock/client/pool cleanup failures (`packages/db/src/migrate.ts:90-144`). Integrity explicitly distinguishes an absent bootstrap ledger, compares ordered `created_at/hash`, fails hash drift, and freezes the exact one-migration artifact set (`packages/db/src/migration-integrity.ts:99-135`, `packages/db/src/migration-integrity.ts:176-224`, `packages/db/src/migration-integrity.ts:255-283`). Real PostgreSQL proof covers bootstrap privilege failure separately and shows partially valid pending SQL plus ledger insert roll back together (`packages/db/test/migration.integration.spec.ts:405-468`). Compiled-path, first/second/concurrent/hash-drift, and pool-closure coverage is included in the final `64/64` unit plus `25/25` integration evidence.

- **Exact DDL and least privilege — PASS.** The literal names, nullability, defaults, checks, FKs, tenant-leading indexes, and epoch uniqueness are represented in the six Drizzle declarations (`packages/db/src/schema/foundation.ts:16-217`) and exercised against PostgreSQL in the retained integration run. Bootstrap creates distinct owner/migrator/runtime roles, gives runtime DML but no schema creation or ledger access, and installs restrictive future defaults (`scripts/postgres/init-roles.sh:35-105`). Runtime CRUD succeeds while runtime DDL and ledger writes fail in retained proof (`docs/reviews/aw-008g-full-evidence-handoff-xhigh.md:31-42`).

- **Scripts, workflow, and card ordering — PASS.** Independent static inspection found exactly nineteen root scripts and exact §10 bodies; the workflow uses the required immutable action SHAs, Node `24.15.0`, pnpm `11.23.0`, frozen install, uncached CI, integration, and hard evidence upload (`.github/workflows/ci.yml:14-39`). Bounded history shows plan/exactness/transitive-build approval commits `c21243c`, `089d89f`, and `1c544e9` before F0 `fa0372f`; D `e84bae5` precedes E `1807283`, which precedes F1 `87eb6b0`. This matches the exclusive ownership and serial `D → E → F1 → G` fence (`docs/plans/aw-008-contracts-db-foundation.md:207-238`). Later corrections are focused; the final property correction changed contracts tests only (`docs/reviews/aw-008g-full-evidence-handoff-xhigh.md:5-9`).

- **Approval-before-implementation/dependencies — PASS.** The same bounded commit order confirms no implementation/dependency bootstrap preceded the reviewed plan and exact dependency/lifecycle decisions. No new dependency was introduced after F0; the final property closure uses the already approved exact `fast-check@4.9.0` pin (`docs/reviews/aw-008g-full-evidence-handoff-xhigh.md:5-9`).

## Final closure condition

Specification closure is complete and this review may be committed. AW-008 remains **RUNNING**, not DONE, until (1) the separate independent quality/security closure is approved and committed and (2) the blocking PR workflow succeeds on the resulting final evidence-only PR head. That final run is the remaining release workflow, not an unresolved AW-008 implementation defect.

Status: **PASS — FINAL-HEAD BLOCKING WORKFLOW REQUIRED BEFORE AW-008 DONE**
